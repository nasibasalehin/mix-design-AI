# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.*
!.env.example
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ML training outputs
# NOTE: For Vercel deployment we keep the trained artifacts tracked,
# so the backend can load the model in production.
# If you want to ignore them again later, uncomment the next two lines.
# ml/concrete_model.pkl
# ml/metrics.json

# Node / frontend
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS & editors
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# Local / agent config (keep secrets out of git)
.claude/settings.local.json
